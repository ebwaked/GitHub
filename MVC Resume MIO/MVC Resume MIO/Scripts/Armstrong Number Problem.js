var armstrong = n;

numbToString (n) {

}

var digit;
function digits ()
{
    for (var digit = 100; i < 1000; i++) 
    {
    
    }
}




var numb = //possibly put the following method inside another function 
var numbToString = function () { //function to turn number to string
    numb = (numb + "").split(""); //turns number to string
    
    for (var i = 0; i < numb; i++) { // loop to iterate 
        numb[i]= parseInt(n[i]); //creates an array of the string and turns it into an array 
                                    // of intergers
    }

}

